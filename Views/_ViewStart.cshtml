@{
    // Define o layout padrão a ser utilizado por esta vista
    // O ficheiro "_Layout.cshtml" será usado como template base
    // Este ficheiro normalmente contém a estrutura HTML comum a todas as páginas
    // (como cabeçalho, menu de navegação, rodapé, etc.)
    Layout = "_Layout";
}
