@model GeoEspectro.Models.Recursos
<!-- Define o modelo de dados usado nesta view: uma instância da classe Recursos -->
@{
    ViewData["Title"] = "Editar Recurso";
    <!-- Define o título da página -->
    var caminho = Model.Tipo == "Imagem" ? "/imagens/" : "/videos/";
    <!-- Define o caminho base do ficheiro consoante o tipo de recurso (imagem ou vídeo) -->
}

<h1 class="mb-4 text-primary fw-bold">@ViewData["Title"]</h1>
<!-- Título principal da página com estilo personalizado -->

<div class="row">
    <div class="col-md-6">
        <!-- Formulário de edição, centralizado numa coluna com 6 unidades de largura -->

        <div class="card shadow-sm border-0">
            <!-- Cartão visual com sombra e sem bordas -->

            <div class="card-body">
                <!-- Corpo do cartão onde está o formulário -->

                <form asp-action="Edit" method="post" enctype="multipart/form-data">
                    <!-- Formulário que envia dados por POST e permite envio de ficheiros -->

                    <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>
                    <!-- Mostra mensagens de validação associadas ao modelo -->

                    <input type="hidden" asp-for="Id" />
                    <!-- Campo oculto com o ID do recurso (necessário para identificar o registo) -->
                    <!-- Campo: Nome -->
                    <div class="mb-3">
                        <label asp-for="Nome" class="form-label fw-semibold"></label>
                        <input asp-for="Nome" class="form-control" />
                        <span asp-validation-for="Nome" class="text-danger"></span>
                    </div>

                    <!-- Campo: Local -->
                    <div class="mb-3">
                        <label asp-for="Local" class="form-label fw-semibold"></label>
                        <input asp-for="Local" class="form-control" />
                        <span asp-validation-for="Local" class="text-danger"></span>
                    </div>

                    <!-- Campo: Ficheiro Atual (imagem ou vídeo) -->
                    <div class="mb-3">
                        <label class="form-label fw-semibold">Ficheiro Atual</label><br />

                        @if (!string.IsNullOrEmpty(Model.Ficheiro))
                        {
                            <!-- Se já existe um ficheiro associado -->
                            if (Model.Tipo == "Imagem")
                            {
                                <!-- Se o tipo for imagem, mostra uma miniatura da imagem -->
                                <img src="@($"{caminho}{Model.Ficheiro}")" alt="Imagem atual" class="img-thumbnail mb-2" style="max-width: 100%; height: auto;" />
                            }
                            else
                            {
                                <!-- Se for vídeo, mostra um leitor de vídeo com o ficheiro atual -->
                                <video width="100%" height="auto" controls class="mb-2">
                                    <source src="@($"{caminho}{Model.Ficheiro}")" type="video/mp4">
                                    O seu navegador não suporta vídeo.
                                </video>
                            }
                        }
                        else
                        {
                            <!-- Se não existir ficheiro, mostra uma mensagem -->
                            <p class="text-muted fst-italic">(Nenhum ficheiro disponível)</p>
                        }

                        <!-- Campo: Substituir ficheiro -->
                        <label class="form-label mt-2">Substituir Ficheiro</label>
                        <input type="file"
                               accept=".png,.jpeg,.mp4,.ogg,.webm"
                               name="novoFicheiro"
                               class="form-control" />
                        <span asp-validation-for="Ficheiro" class="text-danger"></span>
                    </div>

                    <!-- Campo: Observação -->
                    <div class="mb-3">
                        <label asp-for="Observacao" class="form-label fw-semibold"></label>
                        <input asp-for="Observacao" class="form-control" />
                        <span asp-validation-for="Observacao" class="text-danger"></span>
                    </div>

                    <!-- Campo: Autor -->
                    <div class="mb-3">
                        <label asp-for="AutorFK" class="form-label fw-semibold"></label>
                        <select asp-for="AutorFK" class="form-control" asp-items="ViewBag.AutorFK">
                            <option value="0">Escolha um utilizador</option>
                        </select>
                        <span asp-validation-for="AutorFK" class="text-danger"></span>
                    </div>

                    <!-- Botões de ação -->
                    <div class="d-flex justify-content-between">
                        <input type="submit" value="Guardar Alterações" class="btn btn-primary" />
                        <!-- Botão para submeter o formulário -->

                        <a asp-action="Index" class="btn btn-secondary">Cancelar</a>
                        <!-- Botão para cancelar e voltar à lista -->
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <!-- Secção de scripts, onde são carregados scripts de validação do lado do cliente -->
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
